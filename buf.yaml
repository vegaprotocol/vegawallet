---
version: v1beta1

build:
  roots:
    - internal
    - third_party/proto
lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - WIRE_JSON
